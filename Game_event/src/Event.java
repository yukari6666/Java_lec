import java.util.Timer;
import java.util.TimerTask;
import java.util.Random;

public class Event {
	Timer m_Timer = new Timer();
	TimerTask m_Task = new TimerTask() {
		Real_Event event = new Real_Event();
		event. Events();
	};
}
